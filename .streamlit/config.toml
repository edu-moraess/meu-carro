[theme]
# Keep Streamlit's native theme selector available (light / dark / system).
# MOVEXA brand accent inspired by the cyan/blue logo.
primaryColor = "#16C7D9"
font = "sans serif"

[server]
headless = true
maxUploadSize = 10
